# 构建产物
dist/
node/ui-dist/

# 依赖
node_modules/

# WebUI 运行时工作目录（状态库 / 上传 / 日志）
webui-run/

# 日志与临时文件
*.log
nonexistent-config-test.yaml

# 引擎运行时在 node/ 目录生成的默认配置（仓库根 config.yaml 为交付基线）
node/config.yaml

# IDE 与系统
.idea/
.vscode/
.DS_Store
Thumbs.db

# 内嵌 WebUI 构建产物（占位页除外，见 internal/webserver/static/placeholder.html）
internal/webserver/static/*
!internal/webserver/static/placeholder.html
