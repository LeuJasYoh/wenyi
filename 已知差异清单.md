# 已知差异清单

> 对照任务书 §8 DoD 第 8 条：逐条对应架构分册 §7 差异声明或审读报告缺口处置，无未声明差异。
> 状态标记：✅ 已处置 ｜ ⏳ 待阶段落地后补充

## A. 架构分册 §7 行为差异声明（9 条，全部适用）

1. **PDF 引擎**：weasyprint/fpdf2 → QuestPDF（pdf/Wenyi.Pdf）。页码位置、孤儿行控制为近似语义；A5/边距/页脚页码/CJK 字体发现已实现；不做逐字节验收（冒烟测试通过）。`--pdf-engine` 合法值 {weasyprint|fpdf2|questpdf} 三者均路由 QuestPDF，缺省 questpdf 语义（CLI 缺省 weasyprint 保持原版字面）。✅
2. **JSON 修复**：Go 手写最小修复器（围栏剥离/首尾容器提取/字符串内未转义引号转义/尾随多余括号忽略/EOF 补全）弱于 json-repair 包——由 Reviewer 拆块重试降级路径吸收，最终行为等价。✅
3. **HTML 序列化**：自写 bs4 风格序列化器（空元素 `<img .../>`、属性按插入序）；与 bs4 字节级差异属合法序列化差异，验收以语义等价 + 断言为准。✅
4. **编码探测**：jschardet + BOM→声明→探测→UTF-8 replace 固定链路；与 UnicodeDammit 在罕见编码上可能不同，兜底链保证不崩溃。✅
5. **随机性**：回译抽样未播种（与原版一致）；对照比较需注入固定随机源。✅
6. **时间戳**：events.jsonl ts、review 目录名天然不同；比对器（未用，断言式验收）忽略。✅
7. **WebUI**：原库无 GUI；WebUI 为纯增量层（写操作全部 spawn 引擎、术语库直连仅查看+resolve）。阶段 7。⏳
8. **运行时依赖**：Node ≥20 / .NET 10 Runtime；Go 静态。✅
9. **--progress-line**：引擎侧新增全局标志（默认关闭）；WebUI 后端 spawn 恒带并逐行解析为 job.progress → SSE。已实现并测试。✅

## B. 审读报告 §10.2 不可恢复缺口（处置已按规则执行）

1. **_BILINGUAL_CSS/_PRINT_CSS 全文**：按主规格 §13.5 语义重写（.tn-source 灰色淡化/dark mode/iBooks class）；dark-mode CSS 存在性断言通过。✅
2. **review_loop 仲裁后半（normalize 步骤3+/build_conflict_groups/apply/Arbiter）**：以主规格 §12.6 + test_review_agent 断言为真源实现。阶段 4。⏳
3. **_segment_content 核心算法**：按主规格 §7.6.3 + test_ingest 全部 EPUB 边界断言实现（31 用例全绿，含 ruby/化学式/range marker/内联保留/跨资源章）。✅
4. **UnicodeDammit**：固定链路（BOM→XML/HTML 声明→jschardet→UTF-8 replace），Shift_JIS 用例通过。✅
5. **review_evidence/review_run**：以主规格 §12.1–12.3 + 断言为准实现。✅
6. **about.py 文案 / _detect_language_ai system 提示词**：文案以 test_assemble 关于页挂载断言为下限（含"文译/Wenyi/GitHub 反馈"字样）；语言识别 system 为自拟文本（含"语言识别器"路由标记 + {"language": ISO639-1} 协议），测试按路由匹配通过。✅（已登记）

## C. 口径统一表（任务书 §10.1）执行情况

| 项目 | 执行 |
|---|---|
| 模板 27 个 | promptdata/ 27 个 .txt 逐字嵌入（脚本从分册提取）✅ |
| 测试 18+3 辅助 | 18 个 test_*.py 为迁移范围 ✅ |
| provider 8 个/9 键 | factory 分发表 + gemini|google 双键 ✅ |
| manifest initialized + meta.epub_schema | 顶层 bool + meta int=4 ✅ |
| 用量成本 | usage 无成本字段，仅 token（成本为可选增强未实现）✅ |
| pdf-engine {weasyprint\|fpdf2\|questpdf} | CLI 校验三值均合法，路由 QuestPDF ✅ |
| SSE 多订阅 / Job kind 无 glossary_resolve | 已实现（多订阅 fan-out；resolve 为同步端点）✅ |
| summary 6 键 / 单引号 3 分支 | extractor 6 键实现；punct 3 分支（词内/词尾撇号合并为 ’，引语配对）✅ |

## D. 复刻过程中的实现级差异（本轮新增登记）

1. **render_glossary 标签格式**：主规格 §9.3 嵌套括号 `（type（gender，读音:x））` vs 分册 05 扁平 `（type，gender…）`——无测试可裁决，按主规格优先实现。✅
2. **Go CLI chapter+收尾选项冲突退出码**：Python 该路径为 ValueError→CliRunner exit 1（非显式 exit 2），Go 对齐为 1；格式/引擎非法为显式 exit 2。✅（test_cli 断言为准）
3. **锁语义**：Go 书级锁在进程内额外用全局互斥（跨 RunStore 实例），跨进程用 OS 锁（Windows LockFile / POSIX flock）；msvcrt.locking 的 10×1s 重试语义等价实现。✅
4. **usage_delta 去零槽位**：totals 全零时移除键（merge 侧容错）；Python 行为以 test_usage 断言等价迁移。✅
5. **CLI 人读输出**：Rich 表格/进度条为终端 UI 专属，Go 侧以纯文本输出等价信息（进度条语义由 --progress-line JSON Lines 承接，阶段 7）；不影响退出码/路径/事件契约。✅
6. **normalize_zh 半角点号边界**：`v2.版本`/`Mr.王` 保护分支按主规格 §14 例外实现（左侧 ASCII 字母数字且右侧 CJK 不转）。✅
7. **Node 侧码点语义**：JS \d ≠ Unicode Nd（全角数字）——全部正则改 \p{Nd}+u（test_ingest range 注释号用例验证）。✅

## E. 阶段 7 实现级登记（2026-08-26）

8. **引擎新增 --state-dir 全局标志**：WebUI 后端 spawn 时覆盖 config 的 paths.state_dir（PersistentPreRun 应用；默认空不影响 CLI）。为分册 10 §1.1 启动参数契约的引擎侧对偶，登记为第 10 条引擎增量。
9. **WebUI 后端测试环境注入**：buildServer 接受 engineEnv 透传给引擎 spawn（生产传 process.env；测试注入 WENYI_FAKE_ROUTING/WENYI_NODE_CLI 驱动 fake 闭环）。
10. **前端构建产物**：node/ui-dist/ 随包分发（Vite 构建 ui/ 产出）；运行时零构建。
